# Shell.UI.Web

Placeholder web UI workspace for Shelled.

The final UI will be implemented as a SPA that communicates with the native bridge.
This `package.json` lets us add frontend tooling incrementally.
